"""
This module contains the iterator tools which will ease performing sequence processing.
"""
from typing import Iterable


def dataset_generator(category_items: set[str]) -> Iterable[str]:
    """
    Inspect each pattern from the
    category.
    """


def literal_data_generator(inp: "Input") -> Iterable[str]: # type: ignore
    """
    Inspect each literal in the Input
    object.
    """